export type CoordinateType = {
    latitude: string;
    longitude: string;
}
export type LocationType = {
    id: string,
    longitude: number,
    latitude: number,
    pincode: string,
    state: string,
    district: string,
    country: string
}